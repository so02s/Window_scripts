# Создание папок и файлов в них

# ПЕРЕХОД ПО ДИРЕКТОРИИ ВВЕРХ НЕ РАБОТАЕТ
# переделать потом

# + можно в вызове добавить кол-во папок и файлов, их название
# и в целом это переписать на py

create:
	for /l %%i in (00,1,10) ; \
	do \
		pushd && \
		mkdir ex0%%i && cd ex0%%i && nul > day02_ex0%%i.sql && \
		popd ; \
	done
